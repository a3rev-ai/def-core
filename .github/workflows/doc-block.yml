name: Doc Block

# Requires PRs that touch user-facing paths to resolve the ## Documentation block
# (either tick N/A or fill Target + What to document). Feeds the platform-docs curator.

on:
  pull_request:
    branches: [main]
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read
  pull-requests: read

jobs:
  doc-block:
    runs-on: ubuntu-latest
    steps:
      - name: Require a resolved Documentation block on user-facing changes
        env:
          PR_BODY: ${{ github.event.pull_request.body }}
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
          CHANGED_COUNT: ${{ github.event.pull_request.changed_files }}
        run: |
          CHANGED="$(gh api --paginate "repos/$REPO/pulls/$PR_NUMBER/files" --jq '.[].filename')"
          echo "Changed files:"; printf '%s\n' "$CHANGED"
          CHANGED="$CHANGED" python3 - <<'PY'
          import os, re, sys
          changed = [l.strip() for l in os.environ.get("CHANGED", "").splitlines() if l.strip()]
          count = int(os.environ.get("CHANGED_COUNT") or 0)
          if not changed and count > 0:
              print("::error::Could not list the %d changed file(s); failing closed. Re-run this check." % count)
              sys.exit(1)
          IN_SCOPE = ("includes/", "assets/", "templates/")
          in_scope = sorted({f for f in changed if f.startswith(IN_SCOPE)})
          if not in_scope:
              print("No user-facing paths touched (includes/, assets/, templates/) — block not required.")
              sys.exit(0)
          body = os.environ.get("PR_BODY") or ""
          na = re.search(r'-\s*\[\s*[xX]\s*\]\s*\*\*N/?A\*\*', body)
          def field(name):
              m = re.search(r'\*\*' + re.escape(name) + r':\*\*[^\S\n]*(.*)', body)
              return m.group(1).strip() if m else ""
          def filled(v):
              return bool(v) and not (v.startswith('<') and v.endswith('>'))
          documented = filled(field("Target")) and filled(field("What to document"))
          if na and documented:
              print("::error title=Documentation block contradictory::The block is BOTH a ticked N/A and a filled active block — pick one.")
              sys.exit(1)
          if na:
              print("Documentation block resolved (N/A).")
              sys.exit(0)
          if documented:
              # Tier gate — THIS repo's surfaces can only front these corpus
              # tiers (tiers.py nesting: Sam sees site_admin only; Tess
              # +tenant; Pip all). Placeholder / multi-option values fail.
              ALLOWED = {"site_admin"}
              tier_raw = field("Tier")
              if "|" in tier_raw or (tier_raw.startswith('<') and tier_raw.endswith('>')):
                  print("::error title=Documentation Tier unresolved::**Tier:** is still the template placeholder (%r) — pick exactly ONE." % tier_raw)
                  sys.exit(1)
              m2 = re.match(r'[\s`*_]*([A-Za-z_]+)', tier_raw)
              tier = m2.group(1).lower() if m2 else ""
              if tier not in ALLOWED:
                  print("::error title=Documentation Tier invalid::**Tier:** must be one of %s for this repo (got: %r)." % (" | ".join(sorted(ALLOWED)), tier_raw))
                  print("Tier by WHICH SURFACE fronts the feature: def-core/WordPress-side -> site_admin (Sam's tier; anything else hides it from Sam); Tenant Portal -> tenant (Tess); Partner Portal -> partner (Pip). See review-panel STEP 4.")
                  sys.exit(1)
              print("Documentation block resolved (documented, tier=%s)." % tier)
              sys.exit(0)
          print("::error title=Documentation block unresolved::This PR changes user-facing paths but the ## Documentation block is not resolved.")
          print("Touched in-scope paths: %s" % ", ".join(in_scope))
          print("Fix: in the PR description, tick the **N/A** box, or fill **Target** and **What to document** (with a valid **Tier:**).")
          sys.exit(1)
          PY
