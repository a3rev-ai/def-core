You are Claude Code running in the def-core WordPress plugin repo.

Open and follow these files as the only source of requirements:
- docs/prd/PRD-STAFF-AI-FRONTEND-V1.md
- docs/prd/RALPH-RUNBOOK-STAFF-AI-FRONTEND-V1.md

Execute ONLY: LOOP 4 — Frontend Adapter (def-core ↔ Python API).

CRITICAL SCOPE BOUNDARY (adapter-only):
- Loop 4 must implement ONLY the WordPress-side adapter/proxy layer used by the Staff AI frontend.
- Do NOT add or change any UI features in this loop.
- Do NOT implement conversation history, sharing UI, export UI, tool cards, or escalation UI (those are other loops).
- Do NOT store conversations/messages/transcripts in WordPress (no sessions, transients, CPTs, tables).
- Do NOT invent backend endpoints or payload fields. Follow the existing API contract and existing repo patterns.

Adapter Responsibilities (must implement):
1) Collect user context for every request:
- wp_user_id
- email
- capabilities list (must include def_staff_access and/or def_management_access as applicable)
- tenant_id (if the repo already uses a tenant concept; otherwise do not invent)

2) Local security gate:
- Only allow authenticated users.
- Require def_staff_access OR def_management_access BEFORE proxying any request.
- Deny locally with a safe error response if missing.

3) Backend proxy calls:
- Implement proxy methods that call the Python backend endpoints required by the Staff AI UI:
  - list conversations
  - create conversation
  - load conversation
  - send message
  - share
  - revoke share
  - export
  - escalate
- Use the existing def-core pattern for outbound HTTP calls (wp_remote_get/wp_remote_post or existing wrapper).
- Include channel=staff_ai and user identity context in requests.
- Do NOT change backend semantics; this is a pass-through with validation.

4) Consistent response shape:
- Return consistent JSON structures back to the frontend JS.
- Map backend errors into clean UI-safe errors.
- Do not leak stack traces, secrets, or raw exception dumps.

5) Choose ONE WordPress request entrypoint pattern:
- Use existing plugin conventions (WP REST routes OR admin-ajax).
- Do NOT introduce a second parallel proxy mechanism.

Hard Constraints (must obey):
- Smallest possible diff
- Modify existing files only unless absolutely necessary
- Do NOT create new directories
- Do NOT refactor unrelated code
- Do NOT change backend API contracts/endpoints/payloads
- Do NOT persist any chat history/transcripts in WordPress
- If anything about endpoint paths/auth headers/base URL is unclear in the repo, STOP and ask rather than guessing.

Verification (include in your response):
- Describe how to verify at least two adapter calls work end-to-end (suggested: list conversations + send message)
- Confirm unauthenticated is blocked locally
- Confirm missing capability is blocked locally
- Confirm no persistence was added to WP

Stop when complete and output exactly this token on its own line:
STAFF_AI_F4_COMPLETE
